# 자기소개서

## ABOUT ME

데이터가 수집되어 분석과 AI 모델에 닿기까지의 경로를 만드는 일을 해왔습니다. 법학을 전공한 뒤 개발로 진로를 바꿔 커리어를 시작했고, 재직 중이던 2022년에 컴퓨터공학 학사 과정을 마쳤습니다. 6년 5개월간 공공·금융·제약 도메인에서 ETL 파이프라인과 데이터 마트, 분석 환경을 구축했고, 현재는 사내 AI 챗봇 서비스의 수집·적재 계층을 단독으로 맡아 웹 크롤링부터 이기종 RDBMS ETL, LLM 연동까지 전 구간을 설계·구현하고 있습니다. 데이터 엔지니어링에서 출발해 AI 파이프라인까지 이어지는 흐름을 하나로 이해하고 다룰 수 있다는 점이 제 강점입니다.

---

## 1. 수집부터 적재까지, 데이터 계층 전체를 설계했습니다

현재 담당하는 AI 챗봇 프로젝트에서 수집·적재 파트를 단독으로 맡고 있습니다.

외부 수집은 5개 도메인 25개 게시판을 대상으로 하는 크롤링 파이프라인입니다. 게시판마다 날짜 표기와 페이징 방식, 상세 진입 구조가 모두 달랐기 때문에, 25개 크롤러가 동일한 실행 계약을 공유하되 사이트별 차이는 각자 흡수하는 구조로 설계했습니다. 공통화 범위를 날짜 계약으로만 한정한 것은 의도적인 판단이었습니다. 구조가 제각각인 상황에서 무리하게 공통 엔진을 만들면 오히려 오류를 만든다고 봤기 때문입니다. 날짜 파싱이 실패하면 예외를 던지는 대신 해당 행만 제외하도록 설계해, 날짜 판별 실패가 조용히 전체 수집으로 번지는 최악의 상황을 구조적으로 막았습니다.

규제 정보는 본문이 "붙임 참조" 한 줄이고 실제 내용은 첨부에 있는 경우가 많았습니다. 그래서 PDF·HWP·HWPX·DOCX·XLSX·ZIP 6종 포맷의 본문 추출 엔진을 함께 구현했습니다. 확장자는 `.hwp`인데 실제 내용은 HWPX인 파일처럼 형식이 어긋난 첨부까지 헤더로 판별해 처리하도록 만들었습니다. 수집 대상이 고시·공고와 허가사항 변경명령 같은 규제 문서였던 만큼, 법령 문서의 구조에 익숙했던 점이 어떤 정보를 어느 단위로 잘라내야 하는지 판단할 때 실제로 도움이 됐습니다.

내부 수집은 Apache NiFi로 EDMS·LIMS·MES·QMS 등 6개 기간계 시스템(Oracle 3, MSSQL 3)의 데이터를 Hive와 MySQL로 옮기는 파이프라인입니다. 대상 테이블이 2,000개를 넘어 수동으로는 감당할 수 없는 규모였기 때문에, 시스템 카탈로그 메타데이터를 추출해 Hive External Table DDL을 자동 생성하는 방식으로 해결했습니다. MSSQL과 Oracle의 타입 체계가 달라 반복되던 적재 오류는 전 컬럼을 STRING으로 통일하고 타입 변환을 분석 계층에 위임하는 방식으로 정리했습니다.

---

## 2. 문제의 원인은 추측이 아니라 실측으로 찾습니다

AI 요약 단계에서 타임아웃과 서버 오류가 반복됐습니다. 처음에는 첨부 파일 크기를 원인으로 보고 타임아웃 상향을 검토했습니다. 그런데 본문과 공고문은 다섯 줄 안팎인데 컨텍스트가 초과되는 것이 설명되지 않았습니다.

실제 입력값을 측정해보니 원인이 달랐습니다. 자재 마스터 12,336건을 대형 표에 부분 문자열로 매칭하면서 **표 전체가 통째로 LLM 입력에 들어가고** 있었습니다. 검색 전략 자체를 바꿔, 첨부에 자사 상호가 이미 적혀 있으면 자재명 리스트 대신 회사명으로만 검색하도록 했습니다. 그 결과 LLM 입력이 79,692자에서 514자로, PDF는 64,026자에서 223자로 줄었고 장애는 근본적으로 해소됐습니다.

비슷한 경험이 하나 더 있습니다. 분명히 수정한 코드가 운영에 반영되지 않는 문제를 추적한 끝에, 첨부 추출 캐시가 파일 해시만 키로 사용해 "파일은 그대로인데 추출 코드가 바뀐" 상황을 인지하지 못한다는 것을 확인했습니다. 사람이 버전을 올리는 방식은 한 번 잊으면 같은 사고가 그대로 재발하기 때문에 채택하지 않았고, 대신 소스 파일의 해시를 캐시 버전으로 자동 계산하도록 바꿨습니다. 코드가 한 글자만 바뀌어도 캐시가 스스로 무효화됩니다.

증상을 덮는 대신 원인을 구조로 제거하는 것이 제가 문제를 다루는 방식입니다.

---

## 3. 되돌릴 수 있고 검증 가능한 시스템을 만듭니다

운영 DB에 직접 적재하는 구조였기 때문에, 잘못된 데이터가 들어갔을 때의 검증 수단과 복구 수단이 모두 필요했습니다.

적재 실행을 preview·rollback·commit 세 모드로 나누고, 세 모드가 **동일한 준비 객체를 공유하도록** 설계했습니다. 미리보기로 검토한 값과 실제 적재되는 값이 다를 수 있는 여지를 아예 없애기 위해서였습니다. 적재일 단위로 되돌리는 삭제 기능도 별도로 만들어, 사고가 나더라도 복구 경로가 남아 있게 했습니다.

실패 처리도 성공과 실패 두 가지가 아니라 세 단계로 나눴습니다. 이전에는 25개 게시판 중 한 행만 실패해도 파이프라인 전체가 실패로 표시돼, 정상 적재된 나머지까지 수십 분씩 다시 돌리고 있었습니다. 종료 코드를 나누고 게시판별 집계 로그를 남기면서 실패한 게시판만 선별해 재실행할 수 있게 됐습니다.

여기에 실제 DB나 네트워크 없이 동작하는 단위 테스트 71개를 구축했습니다. 버그를 고칠 때마다 재발 방지 테스트를 함께 남기는 것을 원칙으로 삼고 있고, 정책이 바뀌면 그에 맞춰 기존 테스트를 반대로 뒤집는 작업까지 포함해 관리하고 있습니다.

---

## 4. 6년의 데이터 엔지니어링 기반 위에 AI를 얹겠습니다

지금의 작업은 이전 6년의 연장선에 있습니다.

한국환경공단에서는 요소수 수급 이슈에 대응하기 위해 경유차 763만 대의 SCR 장착 현황과 배출가스 등급별·지역별 분포를 분석하는 대시보드를 기획하고, 11개 화면과 121개 차트의 SQL을 작성했습니다. 그 전 단계에서는 이기종 소스로 들어오는 자동차 환경 데이터의 표준화를 수행해, 이후 분석의 기반이 되는 데이터 품질을 확보했습니다. 한국사회보장정보원에서는 54개 데이터 마트를 생성하고 보고서와 저장 프로시저를 개선했으며, LG전자 데이터 품질 진단에서는 품질 현황을 분석하고 개선 방안을 도출했습니다.

이 경험들이 지금 하는 일과 그대로 이어집니다. 데이터가 어떤 구조로 쌓여야 나중에 쓸 수 있는지를 아는 것은 AI 파이프라인에서도 똑같이 필요합니다. 검색이 잘 되지 않는 RAG는 대부분 모델이 아니라 데이터 정제와 구조의 문제이고, 그 문제를 다루는 것이 제가 6년간 해온 일입니다.

입사 후에는 먼저 회사의 데이터 구조와 도메인을 파악해 기존 파이프라인을 안정적으로 이어받겠습니다. 그다음에는 수집·정제·적재 계층을 견고하게 만드는 데서 출발해, 그 위에서 AI가 실제로 동작하게 만드는 엔지니어로 기여하고 싶습니다.
