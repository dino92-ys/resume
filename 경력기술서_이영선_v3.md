# 경력기술서

## 이영선 (Lee Yeong Seon) | Data Engineer | AI/ML Engineer

- **Email**: dldudtjs96@naver.com
- **GitHub**: github.com/dino92-ys
- **Blog**: dino92-ys.github.io
- **총 경력**: 6년 5개월 (2020.03 ~ 현재)

---

## 경력 요약

| 소속 | 직급 | 기간 | 주요 수행 영역 |
| :--- | :--- | :--- | :--- |
| 에이다루트(주) (구 씨씨미디어서비스(주)) | 대리 | 2024.06 ~ 현재 | 데이터 수집·적재 파이프라인, RAG 파이프라인, 데이터 마트 |
| 동양시스템즈(주) | 사원 | 2022.07 ~ 2024.06 | 데이터 표준화, 마트 설계, BI 리포트 구축 |
| (주)유니온시스템즈 | 사원 | 2021.09 ~ 2022.07 | 데이터 품질 진단 |
| (주)씨앤케이피 | 사원 | 2020.03 ~ 2021.09 | ETL, 데이터 시각화, 웹 개발 |

---

# 에이다루트(주) (구 씨씨미디어서비스(주)) | 대리 | 2024.06 ~ 현재

## 1. HK이노엔 AI 챗봇 — 수집·적재 파트 단독 담당

| 항목 | 내용 |
| :--- | :--- |
| 수행 기간 | 2026.04 ~ 현재 |
| 역할 | 수집·적재 파트 **단독 설계·구현·운영 이관** (외부 수집 / 내부 수집 전 영역) |
| 기술 스택 | Python 3.12, Selenium, BeautifulSoup, pypdf, Apache NiFi, Hive/HDFS(Beeline JDBC), MySQL, Oracle, MSSQL, SQLite, Bash, Flowise(LLM API), Rocky Linux |

### 프로젝트 개요

사내 AI 챗봇 서비스에 공급할 데이터를 확보하는 수집·적재 계층을 담당했습니다. 외부 규제 정보를 웹에서 수집·정제해 AI 요약과 함께 적재하는 **외부 수집**과, 사내 기간계 시스템 데이터를 데이터 레이크와 마트로 옮기는 **내부 수집** 두 축으로 구성됩니다.

```
[외부 수집]  5개 도메인 25개 게시판 → 크롤링 → 첨부 본문 추출 → 자사 관련 내용 선별
                                                        → Flowise LLM 요약 → 사내 DB 적재

[내부 수집]  Oracle 3 · MSSQL 3 (기간계) → Apache NiFi ─┬→ Hive / HDFS (ODS)
                                                       └→ MySQL (ODS)  →  MySQL (최종 마트)
```

---

### 1-1. 외부 수집 — 다중 소스 크롤링 파이프라인 설계 및 구현

**과제**
규제·허가사항 변경 정보를 매일 사람이 5개 사이트를 직접 돌며 확인하고 있었습니다. 게시판마다 날짜 표기(`2026-05-06`, `2026.5.6`, `2026/05/06`, `2026.5.6.`), 페이징 방식, 상세 진입 구조가 모두 달랐습니다.

**수행 내용**
- 25개 게시판 크롤러가 `--run-date from A to B` **단일 실행 계약(CLI Contract)** 을 공유하도록 공통 모듈을 설계하고, 사이트별 차이는 각 크롤러가 흡수하는 구조로 분리
- 날짜 정규화 함수를 구현해 구분자 유무·끝 마침표·시각 접두 형식을 모두 처리. **파싱 실패 시 예외가 아닌 `None`을 반환**하도록 설계하여 호출부가 "해당 행 제외"로 처리
- 공통화 범위를 날짜 계약으로 한정하여, 사이트별 예외 로직이 공통 코드를 오염시키지 않도록 유지

**성과**
- 25개 크롤러가 동일한 CLI로 동작하는 일관된 실행 체계 확보
- 날짜 판별 실패가 조용히 전체 수집으로 전환되는 **최악의 실패 모드를 구조적으로 차단**

---

### 1-2. 외부 수집 — 6종 문서 포맷 본문 추출 엔진 구현

**과제**
규제 정보는 본문이 "붙임 참조" 한 줄뿐이고 실제 내용이 첨부 문서에 있는 경우가 많아, 첨부를 읽지 못하면 파이프라인 전체가 무의미했습니다. 특히 국내 공공기관 특유의 HWP·HWPX 처리가 필요했습니다.

**수행 내용**
- PDF·HWP·HWPX·DOCX·XLSX·ZIP **6종 포맷 추출기**를 구현하고, 문단·페이지·표를 공통 세그먼트 단위로 정규화
- HWP는 파서 2종 폴백(Fallback)에 HWPML(XML) 별도 판별을 더한 **3단 구조**로 구성. ZIP은 5단계 중첩까지 재귀 전개
- **확장자와 실제 포맷이 불일치하는 첨부**를 처리: `.hwp` 확장자이나 내용은 ZIP 기반 HWPX인 파일을 헤더 시그니처로 판별해 양방향 폴백으로 대칭 처리. OLE2와 ZIP은 시그니처가 구조적으로 겹치지 않아 기존 경로 회귀가 없음을 테스트로 고정
- 미지원 포맷은 빈 결과 대신 **안내 세그먼트를 생성**해 "추출 실패"와 "미지원"을 구분
- 표는 구조를 유지한 Markdown으로 렌더링하여 사람과 LLM이 동일한 형태로 읽도록 처리

**성과**
- 첨부 파싱 실패 제거 및 첨부 내 규제 정보 수집 가능
- 해당 추출 함수가 전 게시판 공용이므로, 타 도메인의 동일 유형 실패에도 즉시 적용

---

### 1-3. 외부 수집 — LLM 입력 컨텍스트 최적화

**과제**
AI 요약 단계에서 타임아웃과 HTTP 500이 반복 발생했습니다. 본문·공고문은 5줄 안팎인데 컨텍스트가 초과되는 원인 규명이 필요했습니다.

**수행 내용**
- 초기에는 첨부 파일 크기를 원인으로 보고 타임아웃 상향을 검토했으나, **실측으로 원인을 재규명**. 자재 마스터 12,336건을 1,650개 품목이 담긴 대형 표에 부분 문자열 매칭하면서 **표 전체가 통째로 LLM 입력에 포함**되고 있었습니다
- "첨부에 자사 상호가 이미 포함되어 있으면 자재명 리스트 대신 회사명으로만 검색"하는 전략으로 전환. 자재명 검색은 본래 회사명이 없는 문서의 대상 여부를 판별하려는 수단이었기 때문
- PDF는 셀 구분 공백이 없는 원본에서 페이지 전체가 뭉개지는 문제가 있어, 문서별 커스텀 정규식 대신 **`pypdf` 표준 layout 모드**를 채택. 문자 좌표 기준으로 시각적 줄 정렬을 재구성하므로 열 구조를 가정하지 않아 임의의 PDF에 일반적으로 적용

**성과**
- HWPX 표 **79,692자 → 514자 (155배 축소)**, PDF **64,026자 → 223자 (287배 축소)**
- 표 구조·캡션·헤더는 유지하여 가독성 손실 없음
- 반복되던 API 타임아웃 장애 근본 해소
- 이후 게시판 화이트리스트 방식에서 **첨부 내용 기반 판정**으로 전환해 전 도메인 공용화

---

### 1-4. 외부 수집 — 적재 파이프라인 및 안전 실행 체계 설계

**과제**
운영 DB에 직접 INSERT하는 구조라, 잘못된 데이터가 적재되었을 때의 검증 수단과 복구 수단이 모두 필요했습니다.

**수행 내용**
- 크롤링 결과와 Flowise LLM 요약 결과를 결합해 사내 DB에 적재하는 파이프라인 구현
- **3단계 안전 실행 모드** 설계
  - `preview` — DB 미접속, 적재 payload만 JSONL로 출력
  - `rollback` — 실제 SQL 실행 후 롤백하여 SQL 계약 검증
  - `commit` — 행 단위 커밋
  - 세 모드가 **동일한 준비 객체를 공유**하도록 설계하여 "검토한 값과 실제 적재값이 다른" 상황을 구조적으로 제거
- 수집관리번호 채번 시 `LIKE` 패턴 매칭에서 `_`가 와일드카드로 해석되어 발생하던 채번 오류를 **컬럼 직접 비교 + `FOR UPDATE` 잠금** 방식으로 전환. 동시 실행 시 번호 충돌 방지 및 자릿수 초과 시 명시적 실패 처리
- 적재일 단위 삭제기를 별도 구현하여 외래키 역순 삭제 및 공유 파일 마스터 보호(`NOT EXISTS`) 처리
- 수집 파이프라인은 append-only INSERT로 한정하고 자동 갱신·삭제를 배제하여, 사고 시 원인 추적 가능성 확보

**성과**
- 운영 반영 전 적재 payload 전수 검토 가능
- 잘못 적재된 날짜를 안전하게 되돌리고 재처리 상태까지 정리하는 복구 경로 확보

---

### 1-5. 외부 수집 — 운영 안정성 및 검증 체계 구축

**수행 내용**
- **종료 코드 3단계 분리** — `0`(전부 성공) / `1`(일부 행 실패, 적재 자체는 정상) / `2`(치명적 전제 실패). Windows·Linux 실행 래퍼가 이를 구분해 해석하도록 수정하고, 게시판별 집계와 실패 상세(게시판·행·단계·URL·사유)를 로그에 직접 출력
- **캐시 자동 무효화** — 첨부 추출 캐시가 파일 해시만 키로 사용해, 추출 **코드**를 수정해도 이전 결과가 재사용되던 문제 발생. 사람이 버전을 올리는 방식은 누락 위험으로 기각하고, **소스 파일 자체의 SHA-256을 캐시 버전으로 자동 계산**하여 코드 변경 시 캐시가 스스로 무효화되도록 구현
- **첨부 다운로드 재시도** — 목록 페칭에는 재시도 패턴이 있었으나 첨부 다운로드는 단일 시도였던 문제를 확인. 첨부 전용 재시도 상수를 분리 신설하고 예외 시 부분 저장 파일 삭제 후 재요청하도록 처리
- **본문 처리 분기 개선** — "본문 영역을 못 찾음"(구조 변경)과 "본문 영역은 찾았으나 텍스트 없음"(이미지 전용 정상 게시물)을 구분하지 않아 정상 게시물이 적재에서 누락되던 문제를 플래그 기반으로 분리
- **단위 테스트 71개 구축** — 가짜 DB 커서로 SQL 호출 순서·파라미터·트랜잭션 경계를 검증하여, 실제 DB·네트워크 없이 회귀 검증 가능. 버그 수정 시마다 재발 방지 테스트를 함께 고정
- API 키·토큰 자동 마스킹으로 오류 로그를 통한 크리덴셜 유출 방지
- Windows(개발)·Rocky Linux(운영) 양쪽 실행 래퍼를 제공하고, 운영 환경 차이(폐쇄망 드라이버 배치, Oracle Instant Client 아키텍처 등)를 스크립트에 명시

**성과**
- 어느 게시판이 왜 실패했는지 로그에서 즉시 확인하고 **해당 게시판만 선별 재실행** 가능. 전체 재크롤링을 반복하던 낭비 제거
- 첨부 누락 재발 방지 및 재시도 동작을 로그로 검증
- 운영 서버에서 파이프라인 정상 동작 확인

---

### 1-6. 내부 수집 — 기간계 시스템 ETL 및 ODS/마트 구축

**과제**
EDMS(문서관리)·LIMS(실험실정보관리)·MES(제조실행)·QMS(품질관리)·영업 등 6개 기간계 시스템(Oracle 3, MSSQL 3)의 데이터를 분석 및 AI 활용이 가능한 형태로 옮겨야 했습니다. 대상 테이블이 2,000개를 넘어 수동 DDL 작성이 불가능한 규모였습니다.

**수행 내용**
- **메타데이터 기반 Hive External Table DDL 자동 생성** — MSSQL `INFORMATION_SCHEMA` + `sys.extended_properties`, Oracle `ALL_TAB_COLUMNS` + COMMENT 뷰에서 카탈로그 정보를 SQL로 추출하고, Python 생성기가 스키마/OWNER 단위로 `CREATE EXTERNAL TABLE` DDL을 자동 생성
- **이기종 타입 체계 통합** — MSSQL·Oracle 간 데이터 타입 다양성으로 반복되던 Hive 로딩 오류를 전컬럼 STRING 통일 전략으로 해소하고, 타입 캐스팅은 분석 레이어(HiveQL)에 위임
- **DDL 파싱 오류 방어** — 원천 COMMENT 내 따옴표·줄바꿈·탭을 정제하는 파이프라인을 구현하고, Windows에서 생성된 파일의 CRLF 개행이 Linux Hive 파싱 오류를 유발하는 문제를 LF 강제 출력으로 처리
- **배치 파이프라인 구성** — `(시스템명, 스키마)` 가변 인자를 순차 처리하는 Bash 파이프라인을 작성하고, 운영 보안 정책에 맞춰 Hive CLI 대신 **Beeline JDBC**로 HQL 실행
- **Apache NiFi 기반 ETL 구성** — 기간계 데이터를 Hive(HDFS)·MySQL ODS로 적재하고, 일부 데이터는 일배치로 MySQL 최종 마트에 구성. NiFi 프로세서 일부는 **Python 파싱 로직을 연계**하여 레코드 내부에 XML이 포함된 데이터를 처리
- **증분 적재 설계** — 초기에는 대상 데이터가 확정되지 않아 전체 적재로 시작했고, 대상 선정 이후 **수정일시 컬럼 기준 증분 적재(Incremental Load)** 방식으로 전환 진행 중

**성과**
- 2,000개+ Hive External Table DDL을 수동 작성 없이 자동 생성
- 이기종 RDBMS(MSSQL·Oracle)를 동일 파이프라인으로 통합 처리
- 초기 데이터 적재 완료, 현재 증분 적재 및 마트 구성 진행 중

---

## 2. 공공기관 AI 챗봇 POC — RAG 파이프라인 설계 및 구축

| 항목 | 내용 |
| :--- | :--- |
| 수행 기간 | 2026.03 |
| 역할 | RAG 기반 검색 파이프라인 설계 및 구축 |
| 기술 스택 | Flowise(AgentFlow), LLM, RAG 아키텍처 |

**수행 내용**
- Flowise AgentFlow 기반으로 하이브리드 검색(Hybrid Search) RAG 파이프라인을 설계·구현
- 내부 지식 베이스 검색 정확도 개선을 위한 검색 전략 구성 및 검증

**성과**
- 내부 정보 접근성 개선 및 업무 대응 시간 단축 가능성 검증

---

## 3. 한국사회보장정보원

| 항목 | 내용 |
| :--- | :--- |
| 수행 기간 | 2025.07 ~ 2026.03 |
| 역할 | 데이터 마트 생성, 보고서 개발, 프로시저 개선 |
| 기술 스택 | Oracle, SQL, UbiReport |

**수행 내용 및 성과**
- 54개 데이터 마트 생성 및 운영
- 보고서 신규 개발 및 개선, 저장 프로시저(Stored Procedure) 수정
- 데이터 표준화 관리 및 DB 관리 수행

---

## 4. 한국은행 CBDC 모니터링

| 항목 | 내용 |
| :--- | :--- |
| 수행 기간 | 2025.03 ~ 2025.06 |
| 역할 | 시스템 모니터링 |

**수행 내용 및 성과**
- CBDC 시스템 실시간 모니터링 수행

---

## 5. 한국환경공단 자동차환경 빅데이터 구축 4차

| 항목 | 내용 |
| :--- | :--- |
| 수행 기간 | 2024.06 ~ 2025.03 |
| 역할 | 정책 현황 분석용 대시보드 기획, 데이터 마트 설계, SQL 작성 |
| 기술 스택 | PostgreSQL, EDB, Exasol, SQL |

**수행 내용**
- 요소수 수급 이슈 대응을 위한 경유차 현황 분석 대시보드 기획
- 경유차 763만 대의 SCR 장착 현황, 배출가스 등급별·지역별 분포 시각화 설계
- 경유차 감소 추이 및 저감장치 부착 현황을 단일 화면에서 파악할 수 있도록 화면 구성
- 분석 요건에 맞춘 데이터 마트 설계 및 차트별 SQL 작성

**성과**
- **11개 화면 기획 및 121개 차트 SQL 작성**
- 정책 담당자의 현황 파악 및 의사결정 지원 대시보드 제공

---

# 이전 경력 (2020.03 ~ 2024.06)

## 동양시스템즈(주) | 사원 | 2022.07 ~ 2024.06

### 6. 한국환경공단 자동차환경 빅데이터 구축 3차 | 2023.08 ~ 2024.06
`PostgreSQL` `EDB` `Exasol` `SQL` — 마트 설계, 데이터 표준화, 분석용 쿼리 작성

- 이기종 소스에서 수집되는 자동차 환경 데이터의 표준화를 수행하여 분석·시각화의 기반이 되는 데이터 품질 확보
- 분석용 데이터 마트 설계 및 **140여 개 차트 쿼리 작성**, 4차 프로젝트 대시보드의 기반 데이터 파이프라인 구축

### 7. NH농협손해보험 농작물재해보험 전산 시스템 고도화 | 2022.07 ~ 2022.12
`SAP BI` `SQL` — 데이터 시각화, 쿼리 작성

- SAP BI 기반 **유니버스 41개** 구축으로 현업의 데이터 접근성 개선
- **정형 리포트 17개, 비정형 리포트 2개** 생성 및 리포트용 SQL 작성

---

## (주)유니온시스템즈 | 사원 | 2021.09 ~ 2022.07

### 8. LG전자 데이터 품질 진단 | 2022.01 ~ 2022.05
`SQL` `DQ#` — 데이터 품질 진단, POC 수행

- 데이터 품질 진단 및 POC 수행, 품질 현황 분석과 개선 방안을 도출하여 진단 보고서 작성

---

## (주)씨앤케이피 | 사원 | 2020.03 ~ 2021.09

### 9. SQDA (람보르기니 딜러사) | 2021.07 ~ 2021.08
`SAP BO` `Java Spring` `SQL` — ETL 및 데이터 마이그레이션, 웹 개발, DB 관리

- SAP BO 기반 ETL 및 데이터 마이그레이션 수행, Java Spring 웹 기능 개선 및 DB 관리

### 10. 한국동서발전 | 2021.03 ~ 2021.06
`Pentaho Spoon` `Tableau` `SQL` — ETL, 데이터 시각화

- Pentaho Spoon 기반 ETL로 데이터 수집·변환·적재 수행 후 적재 결과 검증 및 Tableau 시각화 구현

### 11. 중앙여론심의위원회 유지보수 | 2021.02
`eGovFrame` — 웹 유지보수

- 전자정부 표준프레임워크 기반 웹 기능 유지보수

### 12. SSCL (포르쉐 딜러사) 유지보수 | 2020.03 ~ 2021.01
`SAP BO/BI` `Java Spring` `Oracle` `SQL` — ETL, 데이터 시각화, 웹 개발

- SAP BO 기반 ETL 수행 및 SAP BI 시각화 보고서 제공, Java Spring 웹 기능 개선

---

## 자격증 및 교육

- **데이터아키텍처 준전문가 (DAsP)** — 한국데이터산업진흥원 (2022.12)
- **SQL 개발자 (SQLD)** — 한국데이터산업진흥원 (2021.06)
- **정보처리기사** — 한국산업인력공단 (2019.10)
- **컴퓨터공학 학사** — 학점은행제 (2021.08 ~ 2022.08 졸업, GPA 4.1/4.5)
- **법학 학사** — 조선대학교 (2011.02 ~ 2018.08 졸업)
